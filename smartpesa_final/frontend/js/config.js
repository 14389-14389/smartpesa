// SmartPesa Frontend Configuration
const CONFIG = {
    API_BASE: 'http://localhost:8000/api/v1',
    APP_NAME: 'SmartPesa',
    VERSION: '2.1.0'
};

// Make sure it's available globally
window.CONFIG = CONFIG;
